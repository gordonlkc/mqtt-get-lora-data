The app demonstrates how use MQTT broker to subscribe to Lora data which is processed in Fleet Gateway.

This node js application could be hosted internally that can run in Raspberry Pi.

### Common setup
```npm install```

### Run the program
```node index.js```